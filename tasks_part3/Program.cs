using System.ComponentModel;
using System.Runtime.InteropServices.JavaScript;

namespace tasks_part3;

class Program
{
    static void Main(string[] args)
    {
        // task1
        // Console.Write("enter the first number: ");
        // int st_num = int.Parse(Console.ReadLine());
        // Console.Write("enter the second number: ");
        // int nd_num = int.Parse(Console.ReadLine());
        // int result = Math.Abs(st_num - nd_num);
        // Console.WriteLine($"result of subtraction= {result}");
        
        // task2
        // Console.Write("enter a number: ");
        // double num = double.Parse(Console.ReadLine());
        // Console.WriteLine($"sequare power of {num}= {Math.Pow( num, 2 )} and the the square root of it is {Math.Sqrt(num)} ");
        //
        // task3 
        // Console.Write("dear user enter your full name:");
        // string full_name = Console.ReadLine();
        // Console.WriteLine($"in uppercase: {full_name.ToUpper()}, in lower case:{ full_name.ToLower()}, number of char: {full_name.Length}");
        
        // task4
        // Console.Write("enter the number of days of your  free trial on netflix: ");
        // int trial_days = int.Parse(Console.ReadLine());
        // DateTime today = DateTime.Today;
        // DateTime end_day = today.AddDays(trial_days);
        // Console.WriteLine($"the end date will be on {end_day.ToString("yyyy-MM-dd")}");
        
        



    }
}